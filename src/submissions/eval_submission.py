import time
from pathlib import Path
BASE_DIR = Path(__file__).resolve().parent.parent.parent

def eval_submission(submission_id, extension, problem):
    for i in range(10):
        print("exist")
        time.sleep(1)
    print("mor")
    exit(0)